# duplicate_window

<Reloadable state="reloadable" note="Enforced when a stream is created or updated, so an already-existing stream keeps the value that applied when it was made." />
The maximum duplication window period allowed to be configured on
a stream.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `duration` | Duration as a string with units such as 100ms, 10s, 5m, or 2h. | - |
