# max_ack_pending

<Reloadable state="reloadable" note="Enforced when a consumer is created or updated, so an already-existing consumer keeps the value that applied when it was made." />
Defines the maximum number of in-flight messages allowed to be
configured on consumers.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `integer` | - | - |
