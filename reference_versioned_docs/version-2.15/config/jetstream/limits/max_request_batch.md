# max_request_batch

<Reloadable state="reloadable" note="Enforced when a pull consumer is created or updated, so an already-existing consumer keeps the value that applied when it was made." />
The maximum request batch size allowed to be configured on pull
consumers.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `integer` | - | - |
