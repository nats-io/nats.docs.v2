# max_msgs

<Reloadable state="reloadable" note="Enforced when a batch publish starts, so a batch already in progress is unaffected." />
Messages allowed in a single batch.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `integer` | - | - |
